## 2026-06-14 Alpha提交UI验收测试（完整）

### 代码通读
- 前端：React 18 + TypeScript + Vite + Tailwind CSS，~40源文件
- 后端：Python纯标准库HTTP，14个web模块，LocalSessionManager内存会话
- 认证：POST /api/test_connection，CSRF 64字符token，5min重放窗口
- 提交安全：/api/submit和/api/submit_batch均返回403

### 登录表单条件
- `!connected && !contextFresh` → CredentialQuickStart (Dashboard内嵌)
- `!connected && contextFresh` → LocalCacheSessionCard ("退出本地会话")
- ConfigPanel：`showCredentialEditor = !cacheOnlyMode || temporaryConnectionOpen`
- contextFresh由磁盘缓存决定，清除localStorage无效

### 验收测试结果（e2e_acceptance_full.py）
- **7项通过，0项失败**
- 页面加载：DOMContentLoaded 5394ms, FirstPaint 5492ms
- 错误密码正确拒绝 ✅
- 正确密码认证成功 ✅
- 首次同步完成（15s）✅
- 候选发现/提交就绪面板可访问 ✅

### 发现的UI问题
1. (P2) 侧边栏阶段组默认aria-expanded=false，子项不可见
2. (P2) 生产完成后阶段状态延迟刷新（待解锁→进行中）
3. (P3) ConfigPanel加载时显示"加载中..."，5s+无内容渲染
4. (P3) 配置参数仅在API响应后渲染，初始加载无placeholder

### 验收测试最终状态（18:50）
- 认证系统: ✅ 完好
- 云端同步: ✅ 已修复 (sync heartbeat 120s守护线程)
- Alpha生产: ⚠️ GuidedPipeline 卡在startup认证加载
  - 已修复: guided job heartbeat (30s)已添加
  - 阻塞点: GuidedPipeline.run() 启动时BRAIN API认证耗时>10min
- 候选数据: 0 (因生产未完成)
- 提交审查: 页面可访问但无数据

### 已应用的修复
1. web_sync_job.py: sync job heartbeat (120s) — sync_0003成功运行70min
2. web_run_job.py: guided job heartbeat (30s) — job_0012未被杀，但pipeline卡住
