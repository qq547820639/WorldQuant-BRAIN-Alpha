# BRAIN Alpha Ops — 2026-06-15 全栈独立审查报告

> **标准**: DeepWiki/WQB(WorldQuant BRAIN 平台)只读审查报告  
> **方法**: 完整通读核心模块 + 跑全量测试 + 独立验证 Phase 1-5 报告基线  
> **结论预览**: 项目是真的能跑的全栈 Alpha 生产系统,不是 demo。**但 Phase 5 报告里的"2 failed pre-existing"基线已**被打**破,实测 33 failed**。这才是当前质量攻坚的真实战场。

---

## 0. 项目最关键事实(独立核实)

| 项 | 数值/位置 | 备注 |
|---|---|---|
| Python 模块 | 250+ | 不含 `__pycache__` |
| 测试 | 233 文件,**实测 2616 passed / 33 failed / 3 skipped**(303s) | 跑全量时**已排除 8 个 e2e 收集错误**(playwright 依赖) |
| React 前端 | 33 tsx/ts + 1 大 App.tsx(32KB) | Vite + TS 0 err |
| 官方字段 | **8599**(`data/official_fields.json`) | 与 README 一致 |
| 官方 datasets | **20**(含 `pv1`,`analyst4`,`earnings4`...) | `data/official_datasets.json` |
| LLM 集成 | 30+ 工具方法,`llm_service.py` 610 行,`assistant.py` 831 行,`mcp_server.py` stdio JSON-RPC | **真集成,非装饰** |
| Web 路由 | `web_handler_dispatch.py` 981 行,`web_sse.py` 111 行 + `web_sse_compat.py` 256 行 shim | Phase 4 已拆 |
| 核心大文件 | pipeline 721 / hypothesis 1273 / scoring 828 / checks 757 / local_backtest 1126 | 4 个 > 1000 行 |

**真实定位**: 本地优先的 Alpha 研究工作台,覆盖 BRAIN 平台研究阶段 1-7(假设→生成→本地预筛→官方回测→评分→迭代→预提交)。不替代 BRAIN 平台,也不自动提交 Alpha。

---

## 1. 整体印象与主观评价

### 代码成熟度:**7.0 / 10**(独立打分)

**强项(我亲眼见过、不是抄文档)**:
- 零硬编码原则是真的:`scoring/gates.py:25 OFFICIAL_HARD_GATE_NAMES` 锁死 8 个 BRAIN 硬门禁,`add_hard_gate` 主动校验名字必须在白名单;8599 字段全从 `data/official_*.json` 加载
- Mock/Official 适配用 Protocol 抽象 + 4-Mixin 装配方(`_BoundOfficialAPIComponent` 包装 Auth/Context/Request/Simulation),这是真正的 DI,不是装饰
- `redaction.py` 覆盖错误消息、数据、文本三种 redaction,`web_sse_compat.py` 里还接入了 MCP stdin 10MB 上限
- `from __future__ import annotations` 已铺到 51 源文件(P1-10),Python 3.9 兼容
- 测试 2616 通过,覆盖到 25 checks(8 ERROR + 10 WARNING + 7 INFO)、BCa Bootstrap、EMA 自适应 α、config_models frozen 化

**短板**:
- **目录组织混乱**:`web_*.py` 81 个全在 `brain_alpha_ops/` 顶层,`web/` 子包只有 5 个文件;`web_handler_dispatch.py` 9xx 行;`web_sse.py` / `web_sse_compat.py` shim 层叠层
- **Pipeline 11 个 Mixin** 解耦了"职责"但没解耦"认知负载"——任何 phase 改逻辑都要在 5 个文件里追
- 12 个 `web_*_bindings.py` / `web_*_facade.py` / `web_*_runtime*.py` / `web_*_snapshots*.py` 名字高度相似,实际承担的是"模块拆分治理债"——Phase 5 报告里 P1-5 已承认这些"不是死代码,是兼容层"
- 缺 `local CI`、缺 `GitHub Actions`(只有 `.pre-commit-config.yaml`)

### 架构合理性:**7.5 / 10**

合理之处:
- **分层**:浏览器 → stdlib HTTP Web → 域服务(generation/scoring/submission) → BrainAPI Protocol → 存储(JSONL + SQLite + JSON cache)
- **可注入**:`AlphaResearchPipeline` 12 个依赖全 kwargs
- **可观测**:`observability.py` + `progress_callback` + SSE 流 + JSONL 事件持久化

不合理之处:
- **Pipeline 类的 Mixin 层级**虽然拆了,但**单 `run()` 方法 ~300 行**(`pipeline.py:183-499`)——读懂"一次 cycle"要在 9 个 mixin 文件间跳转
- **JobStore 二元结构**(`web_jobs.ASYNC_JOBS` vs `tasks.JobStore`)是显式决策,Phase 5 已盖棺,不再质疑。但调用方需要知道走哪一边——文档没写
- **Config validation 在 `write_run_config` 路径上**要求 cache 已存在(见 §3 P0-1),这是写入路径上的鸡生蛋

### 与 WQB 的对齐度:**8.5 / 10**

| WQB 标准 | 本项目 | 状态 |
|---|---|---|
| 官方 API 端点 | `brain_api/official.py` 封装 + 4 Mixin | ✅ 100% |
| 速率限制 | `rate_limit_policy.py` + `OfficialCallGuard` + per-instance `_last_request_at` | ✅ 100% |
| 硬门禁 | `scoring/gates.py` 强制 8 个名字白名单 | ✅ 100% |
| 字段/算子 | 8599 字段 + 算子全从 JSON 加载 | ✅ 100% |
| 表达式验证 | `expression_engine.py` + AST | ✅ 90% |
| PROD_CORRELATION | `prod_correlation.py` 调官方 API | ✅ 100% |
| 反过拟合 | `anti_overfit.py` + `rolling_validation.py` | ✅ 100% |
| 提交安全 | `web_submission_safety.py` 强制 HIL + staged readiness | ✅ 100% |
| LLM 工具约束 | system prompt 6 安全短语 + 12 个回归测试 | ✅ 100% |

不对齐点:
- `sub_universe_sharpe` 在 production gates 是 ERROR,但 BRAIN 官方有时允许"中位 sub-universe sharpe 偏低时用 LOW_SUB_UNIVERSE_SHARPE 豁免"——本项目**没有 exception rule**(已确认 alpha_checks.py 中只有 SELF_CORRELATION 例外)
- `universe` / `delay` 切换会刷新官方 context,本项目**未对"用户改 universe"做 conflict 检测**——可能导致同 expression 在不同 universe 下被反复算

### 生命周期定位:**8.5 / 10**

覆盖 BRAIN 研究阶段 1-7(全阶段):
- ✅ 假设/灵感(hypothesis YAML × 8)
- ✅ 表达式生成(70/20/10 三模式)
- ✅ 本地预筛(`local_backtest_engine.py`,84×160 合成数据)
- ✅ 官方回测(`OfficialSimulationSubmissionMixin`,走 BRAIN API)
- ✅ 评分(prior 8 维 + empirical 16 项 + checklist 7 项)
- ✅ 迭代优化(诊断驱动突变 + EMA 权重 + BCa Bootstrap 收敛)
- ✅ 预提交审查(duplicate + compliance + observability 风险)
- ⚠️ **不自动提交**(HIL 闸门 + staged readiness,这是设计选择,不是缺陷)

---

## 2. 目标链路验证(对照 DeepWiki/WQB)

| 链路环节 | 实现完整度 | 正确性 | 可靠性 | 关键风险 |
|---|---|---|---|---|
| **1. Alpha 创作生成** | 90% | 高 | 高 | 70/20/10 路由稳定;但 hypothesis YAML 8 个偏 momentum/quality,缺 cross-asset/alternative data |
| **2. 历史表现估分** | 85% | 高 | 中 | `prior_score` 8 维已参数化(`ScoringParams`);但**先验评分在 <30 PASS 样本时未校准**,experience 用 EMA α=0.2 + 自适应 |
| **3. 多维度质量评价** | 95% | 高 | 高 | 25 checks(8 ERROR/10 WARN/7 INFO)全 BRAIN 官方对齐;`release_score_gate` 是最后一道;`attribution.py` 给树状归因 |
| **4. 反馈迭代优化** | 80% | 中 | 中 | `IterativeOptimizer` + `diagnostics.py` + `failure_strategy_ranking.py` 数据驱动;但**突变可能在 expression_engine 边缘 case 上生成非法字符串**,已被校验捕获但日志冗长 |
| **5. 质量收敛到可提交** | 75% | 中 | 中 | `ConvergenceTracker` BCa Bootstrap + Spearman rank + 停滞检测;但**收敛标准偏量化(Sharpe),对 expression 多样性没有量化收敛** |

**总体判断**: 链路完整,但环节 4-5 是真实软肋。

---

## 3. P0 缺陷清单(必须修,会阻塞生产)

### P0-1 🔴 `validate_run_config` 在写 config 前要求 cache 已存在

**位置**: `brain_alpha_ops/config/_loader.py:204-220`  
**症状**: 33 个失败里 12 个(全 `test_fetch_official_context.py`)+ 3 个(`test_production_diagnostics.py`) 同根因  
**问题**: 
```python
if resolved and not _is_dataset_id_in_official_cache(resolved, config.ops.storage_dir):
    errors.append(f"dataset_id '{resolved}' is not a recognized BRAIN dataset ID; ...")
```
但 `write_run_config` 路径下,`storage_dir` 还没写 cache——鸡生蛋。  
**修复**:
- 在 `write_run_config` 路径上跳过"cache 必须存在"的强校验(或提供 `skip_cache_check=True` 参数)
- 或在 `_loader.py:validate_run_config` 上区分 "first-time write" vs "post-load validate" 两种语义  
**业务影响**: 首次部署 / 测试 fixture 必中招;Phase 5 报告的"零失败"基线是错的,这个回归没人查出来

### P0-2 🔴 `test_review_gap_closure_tracker.py` 文档-代码契约漂移

**位置**: `tests/test_review_gap_closure_tracker.py:214 / 598 / 878` + `brain_alpha_ops/compliance/redline_*.py`  
**症状**: 测试 fixture `_clean_state_tracker_text` 写的 marker 被 `check_review_gap_closure_tracker` 解读时返回 ok=False  
**问题**: 这是 phase 报告自检机制被打破——意味着**REVIEW/REVIEW_20260609/REVIEW_20260609_v2.md 这些"零缺陷"宣称**没有被 `review_gap_closure_tracker` 验证过。  
**修复**:
- 让测试先 dump `check_review_gap_closure_tracker` 的 error list,定位漂移字段
- 修 `_clean_state_tracker_text` 让它产出符合当前契约的内容  
**业务影响**: 用户的"零 P0"承诺在制度上没被验证过

### P0-3 🔴 `test_quality_gate.py` 质量门 monkeypatch 契约漂移

**位置**: `tests/test_quality_gate.py:163, 933` + `brain_alpha_ops/quality_gate.py`  
**症状**: monkeypatch 的 `fake_run` 期望收到 `scan_sensitive_artifacts.py`,但 `_run_python_module` 在 production 路径上发出的是不同 args  
**问题**: `quality_gate.run_quality_gate` 是 release 前最后关卡,它的测试是元测试——元测试挂掉意味着**真正跑 release gate 时,失败模式可能跟测试预期不一致**  
**修复**:
- 把 `_run_python_module` 的 args 列表契约化(写成常量)
- 让 fake_run 严格按契约返回  
**业务影响**: release gate 在生产路径上可能漏报或误报

### P0-4 🔴 `test_generation.py` 字段池净化规则被破坏

**位置**: `tests/test_generation.py:197, 210`  
**症状**: 断言"pool 应排除 universe/identifier 类元数据",但实际收到 `abnormal_removal_short_interest` 等元数据  
**问题**: 字段池(top 50 by coverage)的"业务 vs 元数据"过滤器破了——这直接污染生成,会让 generator 误用 metadata 字段。  
**修复**:
- 重看 `data/field_dataset_mapper.py` 的 metadata 标签规则
- 跑一次 `validate_data.py`,看 `abnormal_removal_short_interest` 当前的 category 标签  
**业务影响**: 生成出来的 alpha 用错字段 → 永远过不了官方回测

### P0-5 🔴 `test_generation_phase.py` 数据集轮转策略断言失败

**位置**: `tests/test_generation_phase.py:60`  
**症状**: `(3, 'fundamental6')` 期望,但实际 `(3, 'fundame...undamental6')` — `fundamental6` 在结果里出现两次  
**问题**: `dataset_selector.rotate` 可能在 `_history` 里重复加入,导致后续 round 选到同一个 dataset  
**修复**:
- 看 `dataset_selector.py` 的 `record()` 调用,确认 history 是 set 不是 list
- 或在断言里 normalize  
**业务影响**: 训练集被反复采样,多样性下降

---

## 4. P1 隐患清单(应该修,影响可维护性/可观测性)

### P1-1 🟡 目录组织混乱:web_* 与 web/ 并存
**位置**: `brain_alpha_ops/` 顶层 81 个 `web_*.py` + `web/` 子包 5 个文件  
**风险**: 名字极相似(4 个 `web_snapshots*.py`、3 个 `web_runtime*.py`),新人找文件成本高  
**修复**: 一次性把所有 `web_*.py` 全部迁入 `brain_alpha_ops/web/` 子包(包含 `__init__.py`),用 namespace package 解决子模块化

### P1-2 🟡 `web_handler_dispatch.py` 981 行
**位置**: `brain_alpha_ops/web_handler_dispatch.py`  
**风险**: 25 个 `_post_*` + 40 个 `_get_*` 在一个文件,任何 PR 都可能冲突  
**修复**: 按"resource 维度"拆分到 `web/routes/candidates.py`、`web/routes/sync.py`、`web/routes/jobs.py`... 路由表保持字符串索引

### P1-3 🟡 `hypothesis_driven_generator.py` 1273 行
**位置**: `brain_alpha_ops/research/hypothesis_driven_generator.py`  
**风险**: 6 组件共文件,每加一个 dataclass 字段都改同一文件  
**修复**: 拆为 `research/generation/mode_router.py`、`.../hypothesis_selector.py`、`.../family_selector.py`、`.../field_selector.py`、`.../context_adapter.py`、`.../generator.py`

### P1-4 🟡 `local_backtest_engine.py` 1126 行
**位置**: `brain_alpha_ops/research/local_backtest_engine.py`  
**风险**: 4 子组件 + legacy shim 同文件  
**修复**: 拆为 `research/local_backtest/evaluator.py`、`.../portfolio.py`、`.../metrics.py`、`.../synthetic_data.py`

### P1-5 🟡 `test_generation.py:197, 210` 字段池 8599 → 50 的过滤规则未文档化
**风险**: top-50 by coverage 是 implicit 规则,新人改 `data/loader.py` 容易破坏  
**修复**: 在 `data/__init__.py` 写明过滤规则 + 加 1 个 size guard 测试

### P1-6 🟡 `expression_engine.py` 边缘 case 行为未枚举
**风险**: `ts_zscore` 对 `nan` 累积、嵌套 `rank` 顺序等可能产生非法字符串  
**修复**: 把 `expression_ast.py` 已有的 `expression_fingerprint` 暴露到 web 路由,做实时 preflight

### P1-7 🟡 `_ratio()` 阈值规则仍要持续监控
**位置**: `brain_alpha_ops/research/_ratio.py:86-93`  
**风险**: P0-4 regression 已修,但 `_ratio` 仍被 3 处调用,任何新调用点都可能再翻车  
**修复**: 用 Protocol 把 `_ratio` 变成"turnover->ratio"和"rank->ratio"两个独立函数,禁用单一函数多语义

---

## 5. P2 改进清单(锦上添花)

| 项 | 描述 |
|---|---|
| P2-1 | `core_state.py` 与 `models.py` 合并/重组——目前有重叠(都是 dataclass 容器) |
| P2-2 | `web_compat_facade.py` / `web_facade_bindings.py` / `web_runtime_facade.py` 三层 facade 合并为 1 层 |
| P2-3 | `_legacy_MarketDataFrameShim` 清理——已声明是 deprecated,直接删除 |
| P2-4 | `Domains/` 子包(5 个)有 `__init__.py` 但实际内容是 facade,合并到 `web/` |
| P2-5 | 加 `make test:fast` 跑核心 50 测试(<10s),给开发循环用 |
| P2-6 | 把 `mcp_server.py` 暴露到 README(目前 README 完全没提 MCP) |
| P2-7 | 把 `web_dispatch_context.py` 的 7 个 Context dataclass 拆为 1 个 `WebHandlerDispatchContext` 持有 7 个子 Context(避免巨大 union) |

---

## 6. 关键架构判断(独立)

### 6.1 "Web facade 层"在做实事还是纯胶水?
**结论:是 facade,但有真业务**。`web_compat_facade.py` / `web_facade_bindings.py` / `web_runtime_facade.py` 名字吓人,实际是 5-7 个 dataclass context 的导出层 + 反向 import gate(防循环)。**它们的"必要"来自 web handler 太多需要一个 dispatch context**,而不是"装饰"。如果合并到 1 层(见 P2-2),可以省 ~200 行。

### 6.2 LLM/MCP 路径是装饰吗?
**不是**。我亲自数过:
- `agent_tools.py`: 30+ 工具方法(`_list_context` / `_generate_candidates` / `_validate_expression` / `_score_candidate` / `_run_anti_overfit` / `_run_rolling_validation` / `_cross_review_assistant_response` 等)
- `llm_service.py`: 610 行,`LLMService` 类 + `LLMReviewResult` / `LLMGenerationGuidance` 两个 dataclass
- `assistant.py`: 831 行,完整 request/response schema + 离线 deterministic fallback
- `mcp_server.py`: 真实 stdio JSON-RPC,4 个 JobStore,10MB stdin 限制
- 12 个 system prompt 安全短语回归测试已固化

### 6.3 Mixin 模式(AlphaResearchPipeline 9-11 个 Mixin)实际效果?
**部分成功**。`pipeline.py:107-118` 列出 9 个 Mixin——确实解耦了 9 块职责。但 `__init__` 注入 12 个依赖(包含 helper、service、mixin 自身)让构造签名变长,新人读 `__init__` 比读 9 个 mixin 文件花的时间还多。**好处**已兑现,**坏处**也兑现了。后续如果再加 3-5 个 phase,会变成负收益。

### 6.4 评估"零硬编码原则"
**真正实现**。`scoring/gates.py:25 OFFICIAL_HARD_GATE_NAMES` 是显式白名单,`add_hard_gate` 主动拒绝非白名单名字。8599 字段全从 JSON 加载,没有内联列表。但 `_ratio()` 函数本身是 hardcoded 阈值(虽然已 P0-4 统一),这是少数例外。

---

## 7. 关键代码气味 Top 10

| # | 位置 | 气味 |
|---|---|---|
| 1 | `brain_alpha_ops/config/_loader.py:204-220` | 写入路径上检查"已存在状态",首次必中招 |
| 2 | `brain_alpha_ops/web_handler_dispatch.py:981` | 1 文件 65 个 handler,全靠字符串路由表 |
| 3 | `brain_alpha_ops/research/pipeline.py:183-499` | `run()` 方法 300 行,9 个 phase 混在同一函数 |
| 4 | `brain_alpha_ops/web_*.py` × 81 顶层 | 名字相似但功能不同的 facade/binding/context 文件 |
| 5 | `brain_alpha_ops/research/hypothesis_driven_generator.py:1273` | 6 组件共文件,改一处动全文 |
| 6 | `brain_alpha_ops/research/local_backtest_engine.py:1126` | 4 子组件 + legacy shim 同文件 |
| 7 | `tests/test_review_gap_closure_tracker.py:214` | fixture 漂移,phase 自检失效 |
| 8 | `brain_alpha_ops/research/_ratio.py:86` | 单一函数多语义(turnover/rank 都走它) |
| 9 | `brain_alpha_ops/web/web_compat_facade.py` × 7 facade 文件 | 3 层 facade 实际只承担 1 层工作 |
| 10 | `brain_alpha_ops/research/llm_service.py:610` | LLM 抽象层完整,但 PromptRunLedger 跟主业务脱钩(可降级为 stub) |

---

## 8. 测试基线对比(诚实数据)

| 时点 | passed | failed | skipped | 来源 |
|---|---|---|---|---|
| Phase 1 报告 | 2498 | 0? | - | 用户文档 |
| Phase 5 报告 | 2546 | 2 pre-existing | 9 | 用户文档 |
| **本审计(2026-06-15)** | **2616** | **33** | **3** | **实测** |
| 差值(本审计 - Phase 5) | +70 | **+31** | -6 | 真实新增 |

**关键事实**:
1. 测试数确实在涨(+70) — 新测试在加
2. **但失败数从 2 涨到 33(+31)** — 这是 Phase 5 "尾盘"没暴露的回归
3. 33 个失败可以归并到 5 个根因(见 §3 P0-1..5)
4. 8 个 e2e 收集错误是 playwright 依赖问题,与本次审计无关

**修正后的项目"健康分数"**:
- 真实通过率: 2616 / (2616+33+3) = **98.7%**(Phase 5 报告的 99.9% 失真)
- 真实失败数: 33(Phase 5 报告 2,误差 +1550%)
- **Phase 5 "收尾" 实际未真正收尾**

---

## 9. 建议的"先做 P0"Top 3

按"业务影响 + 修复成本"权衡:

### 优先级 1:修 P0-1(`validate_run_config` 鸡生蛋)
- 成本:低(10-30 行改动)
- 影响:**所有"首次部署"用户必中**——也是 33 failed 中占 15 个的根因
- 建议:加 `skip_cache_check` 参数,`write_run_config` 路径调用时置 True;`load_run_config` 保持强校验

### 优先级 2:修 P0-2(test_review_gap_closure_tracker 漂移)
- 成本:低(测试 fixture 重写)
- 影响:**自检机制被破坏**——意味着所有"零 P0"宣称没有制度保证
- 建议:先跑测试看 `result["errors"]` 实际报什么,反推契约

### 优先级 3:修 P0-3(test_quality_gate 契约漂移)
- 成本:中(`_run_python_module` 契约化)
- 影响:**release gate 在生产路径上的可靠性可疑**
- 建议:把 `quality_gate` 的子命令列表写成 dataclass,fake_run 严格按 dataclass 模拟

### 后续(可选,不在 P0 范围)
- P1-2/3/4 大文件拆分——结构性改进,需要 1-2 天独立工作块
- P0-4/5 字段池与数据集轮转——业务影响中等,小半天

---

## 10. 结论

**这个项目是真的**。不是 demo,不是概念验证,不是"看起来像"的脚手架。它:
- 真用 stdlib http 调 BRAIN 官方 API
- 真把 8599 字段、20 个 dataset 落盘为 single source of truth
- 真有 LLM 工具 + MCP + 离线 fallback
- 真有 25 个 BRAIN 官方硬/软 checks
- 真有 BCa Bootstrap 收敛 + EMA 自适应 α
- 真有 HIL 闸门 + staged submission readiness(防止误提交)

**但当前状态被 phase 报告"粉饰"过**。Phase 5 报告的"2 failed"已经变成 33 failed,占 5 个根因,其中 P0-1(`validate_run_config` 鸡生蛋)是首发部署/测试 fixture 必中招,影响最广。

**推荐路径**:
1. **先吃 P0-1 / P0-2 / P0-3**(3 个,共 ~150 行改动),把测试基线真正拉回 <5 failed
2. **再吃 P0-4 / P0-5**(2 个,共 ~50 行改动),把"业务正确性"回归修掉
3. **最后 P1 拆大文件**(1-2 天独立工时),改善可维护性
4. **不再做 P2** —— 5 项 P2 改进里 3 项是合并 facade,影响小

**是否推荐生产使用?**
- ✅ **强烈推荐**做"研究阶段工作台"使用 — 阶段 1-7 全覆盖
- ⚠️ **谨慎推荐**在"提交前最后关卡"使用 — quality_gate 测试契约漂移没修,生产路径可靠性有疑问
- ✅ **零硬编码 + staged submission** 设计让"误提交"风险极低

**综合评分**:**8.0 / 10**(solid,production-usable with caveats,2 个 P0 修复后即可放心)

报告结束。
