# Code Review Task Execution Checklist

Updated: 2026-05-25

Source checklist: `docs/COMPREHENSIVE_CODE_REVIEW_20260522.md`

## Completed

- [x] H-01 Remote Web mode can mint sessions for any reachable client
  - Implemented remote admin-token requirement before Web root session creation.
  - Added config validation and focused tests for admin-token handling.
  - Verified Python compilation for `brain_alpha_ops` and `tests`.
- [x] M-01 Assistant context defaults and opt-in flags can expose sensitive local context
  - Changed the core assistant context default to redacted output.
  - Added explicit `--include-sensitive` / `include_sensitive` opt-ins for CLI and agent tools.
  - Added focused test assertions for redacted defaults and explicit sensitive output.
- [x] M-02 SQLite expression lookup falls back to a full-table Python similarity scan
  - Added a bounded SQLite similarity scan with `max_scan_rows`.
  - Exposed scan count and truncation metadata in lookup responses.
  - Added Web API query bounding and focused tests for truncated scans.
- [x] M-03 Residual string-based DOM rendering still depends on convention and regex whitelists
  - Added `scripts/check_frontend_innerhtml.py` as a static guard for approved `innerHTML` sinks.
  - Integrated the guard into `scripts/quality_gate.py`.
  - Verified the current frontend has 22 approved sinks and no unexpected findings.
- [x] L-01 Deprecated command-line credential arguments still exist outside production
  - Command-line credentials are now disabled by default in every environment.
  - Added an explicit `--allow-insecure-cli-credentials` compatibility escape hatch.
  - Added tests for default rejection and explicit opt-in behavior.
- [x] L-02 Cached user-alpha discovery scans and sorts every matching cache file
  - Added a bounded cache discovery limit with a default of 50 recent files.
  - Threaded `max_files` through latest cached alpha helpers.
  - Added a focused cache bound test.
- [x] L-03 Duplicate helper logic can drift
  - Consolidated candidate-pool helper behavior onto `pipeline_helpers`.
  - Removed duplicate `ranking_score`, `expr_key`, `blocked_gate`, and `is_hard_backtest_blocked` implementations from `candidate_pool.py`.
  - Added a shared-helper identity/behavior test.
- [x] L-04 Large coordinator modules remain the main maintainability bottleneck - executable guardrail slice
  - Added `scripts/check_module_size.py` to make oversized-module drift visible in automation.
  - Integrated the module-size audit into `scripts/quality_gate.py`.
  - Added focused quality-gate tests for the new audit step.
- [x] TEST-01 Full pytest blockers from the current review were resolved
  - Removed internal `sensitive_fields_redacted` metadata from assistant request envelopes.
  - Aligned the SQLite scan-limit regression test with the implementation's append-order scan semantics.
  - Verified the two previously failing tests now pass with the workspace test dependency path.
- [x] L-04 Large coordinator modules remain the main maintainability bottleneck - deep architecture refactor current slice
  - Extracted `CycleState` and adaptive strategy reward bookkeeping from `AlphaResearchPipeline` into `brain_alpha_ops/research/pipeline_state.py`.
  - Replaced inline bandit reward and Web runtime summary assembly in `pipeline.py` with focused helper calls.
  - Added focused regression coverage for isolated cycle state defaults, strategy reward calculation, bandit map updates, and runtime summary payloads.
- [x] L-04A Pipeline snapshot and summary serialization extraction
  - Extracted runtime-data, summary, candidate snapshot, and backtest snapshot assembly out of `AlphaResearchPipeline` into `brain_alpha_ops/research/pipeline_snapshot.py`.
  - Kept lifecycle orchestration in `pipeline.py` and preserved current response payload shapes.
- [x] L-04B Pipeline snapshot regression tests and verification
  - Added focused tests for extracted snapshot/summary behavior.
  - Re-ran module-size audit, quality gate, and related pytest slices.
- [x] L-04C Backtest slot snapshot extraction
  - Moved active backtest slot payload rendering to `backtest_slot_snapshot()`.
  - Preserved empty, halted, and active slot response shapes.
- [x] L-04D Remove obsolete inline backtest slot snapshot code
  - Deleted the legacy `_slot_snapshot_legacy_inline()` implementation after `backtest_slot_snapshot()` became the active path.
  - Removed now-unused slot message/progress imports from `pipeline.py`.
- [x] L-04E Web session/security facade extraction
  - Extracted local session policy, CSRF/stream-token validation, remote-admin policy, and cookie helpers into `brain_alpha_ops/web_session.py`.
  - Kept compatibility wrappers in `web.py` so existing route dispatch and tests keep their public import surface.
  - Added focused coverage for session policy, remote-admin token validation, and request-session checks.
- [x] L-04F Pipeline cloud matching/ranking extraction
  - Extracted cloud similarity indexing, cloud self-correlation risk, cloud status matching, accepted-candidate memory, and smart ranking into `brain_alpha_ops/research/pipeline_cloud.py`.
  - Preserved BRAIN compliance semantics: cloud similarity is used to avoid duplicate/self-correlation risk, not to relax official checks or thresholds.
  - Added focused regression coverage for token/AST matching, official-alpha exclusion, status matching, accepted-candidate dedupe, and cloud-risk ranking penalties.
- [x] L-04G Web HTML/CSP boundary extraction
  - Extracted cached HTML loading, session-token placeholder rendering, and default CSP integration into `brain_alpha_ops/web_html.py`.
  - Kept `web.py` wrappers for `_load_html`, `_render_html`, hash helpers, and `content_security_policy_for_html`.
  - Added cache-isolated tests for HTML rendering and CSP hash behavior.
- [x] L-04H Web error/progress presentation extraction
  - Extracted safe error messages, structured Web error payloads, cloud self-correlation guidance enrichment, and progress phase labeling into `brain_alpha_ops/web_errors.py` and `brain_alpha_ops/web_progress.py`.
  - Kept user-actionable auth/credential messages redacted and compatible with existing Web API payload shapes.
  - Added focused tests for rate-limit classification, cloud self-correlation guidance, and progress phase-label fallbacks.
- [x] L-04I Candidate pool backtest-state rule extraction
  - Moved active backtest and pending simulation target rules from `pipeline.py` into `brain_alpha_ops/research/candidate_pool.py`.
  - Kept `AlphaResearchPipeline` as a thin orchestration caller while preserving candidate-pool, pending-backtest, and backtest-slot behavior.
  - Added focused coverage for active, completed, failed, and metriced simulation states.
- [x] L-04J Pipeline official context loading extraction
  - Extracted official-context JSON/API/cache loading, validation-cache helpers, dataset-field checks, and advanced component wiring into `brain_alpha_ops/research/pipeline_official_context.py`.
  - Preserved BRAIN contract semantics: no custom fields/operators, no threshold drift, JSON-first official data, API/cache fallback, Dataset lineage validation, and existing event/progress payload shapes.
  - Added focused coverage for configured official context detection, field/operator/Dataset validation reasons, API fallback, and rate-limit local-cache behavior.
- [x] L-04K Pipeline observability orchestration extraction
  - Extracted observability refresh, failure payloads, duplicate-expression generation guidance, and official-call throttle assembly into `brain_alpha_ops/research/pipeline_observability.py`.
  - Preserved existing monkeypatch surface by injecting the observability snapshot builder from `pipeline.py`.
  - Added focused coverage for guidance application, setter failures, ready throttle payloads, and refresh failure events.
- [x] L-04L Web snapshot runtime wrapper extraction
  - Added `brain_alpha_ops/web_snapshot_runtime.py` as a dependency facade for Web snapshot services.
  - Converted the remaining snapshot wrappers in `web.py` to thin runtime delegations while preserving public function names used by route dispatch, tests, and local scripts.
  - Covered memory/knowledge/observability snapshots, prompt ledger, assistant context/request/response/guidance, anti-overfit, rolling validation, cross-review, latest result/history, durable jobs, and user profile snapshots.
- [x] P1-DATA Official context lineage and freshness evidence
  - Added `brain_alpha_ops/data/official_context_validation.py` and `scripts/check_official_context.py`.
  - Cross-checks official fields/operators/datasets identity uniqueness, metadata hashes and record counts, plus Dataset field-count lineage (`7642 == 7642`).
  - Wired blocking structure/lineage checks into redline-3 and added optional strict freshness enforcement to `scripts/quality_gate.py --strict-official-context`.
  - Updated the one-page diagnosis to separate structural PASS from refresh freshness risk.
- [x] P1-CONTRACT Unified BRAIN contract comparison gate
  - Added `scripts/check_brain_contract.py` to compare red lines, zero-deviation thresholds, Dataset lineage, scoring simulation, frontend sync, and checkpoint/history readiness from the generated diagnostic snapshot.
  - Integrated the contract check into `scripts/quality_gate.py`; default mode blocks P0 contract drift, while `--strict-official-context` also blocks stale/unrefreshed official context evidence.
  - Added focused regression coverage in `tests/test_brain_contract_check.py` and updated quality-gate step-order tests.
- [x] P1-SCORE Parameter trace completeness
  - Added BRAIN alpha `type` into `OfficialScoringSystem` settings trace so scoring output covers the complete platform settings envelope.
  - Added regression coverage in `tests/test_official_scoring_system.py`.
- [x] P1 Official context refresh evidence
  - Refreshed official BRAIN context with credential-backed session-cookie auth on 2026-05-24 Asia/Shanghai.
  - Current official context is sourced from `official_api`, has non-stale metadata, and records fields=7642, operators=66, datasets=16.
  - Strict context validation and strict unified BRAIN contract validation now pass with zero findings.
- [x] L-04M Web CSS/source-size guardrail closure
  - Extracted the Web console design system from `brain_alpha_ops/web/index_template.html` into `brain_alpha_ops/web/css/app.css`.
  - Extended `brain_alpha_ops/web/build_inline.py` so the shipped `index.html` still inlines CSS plus the 16 JavaScript modules for the single-file EXE/web artifact.
  - Reduced `index_template.html` to 439 lines and `web/js/app.js` to 995 lines, bringing both back under the module-size guardrail without changing the BRAIN API contract.
  - Updated frontend module tests and the innerHTML allowlist so source CSS, template structure, generated HTML, and approved DOM sinks are validated together.
- [x] L-04N Frontend app-service boundary extraction
  - Split result snapshot merging into `brain_alpha_ops/web/js/result-state.js`, table/mobile/empty-state rendering into `brain_alpha_ops/web/js/result-table.js`, and form/config payload assembly into `brain_alpha_ops/web/js/form-controls.js`.
  - Reduced `brain_alpha_ops/web/js/app.js` from 995 to 781 lines and removed the custom `app.js` module-size baseline so the default 800-line guard now applies.
  - Preserved the single-file Web artifact by inlining 19 JavaScript modules plus `css/app.css` through `brain_alpha_ops/web/build_inline.py`.
  - Fixed config-to-checkbox hydration by routing control writes through type-aware form helpers instead of writing checkbox booleans to the `value` property.

## Pending

- None for the current local code checklist.

## Deferred

- [ ] L-04 broader deep refactoring beyond the current L-04A through L-04L slices should continue as scoped follow-up work.

## Verification

- [x] `python -m compileall -q brain_alpha_ops scripts tests`
- [x] `scripts/quality_gate.py --skip-tests --json`
  - Passed: Python compile, config validation, dependency policy, redline verification, frontend inline sync, frontend syntax, frontend innerHTML guard, text encoding scan, secret scan, cache metadata audit.
- [x] Focused pytest for the two current blockers
  - `tests/test_cli.py::test_cli_assistant_request_prints_llm_envelope`
  - `tests/test_expression_sqlite_index.py::test_expression_sqlite_index_similarity_lookup_caps_scan_rows`
- [x] `scripts/check_module_size.py --json`
  - Passed: 149 Python files checked, no oversized-module findings.
- [x] `tests/test_quality_gate.py`
  - Passed: 14 focused quality-gate tests.
- [x] `scripts/quality_gate.py --skip-tests --json`
  - Passed with the new `module_size_audit` step included.
- [x] Full pytest suite
  - `609 passed, 1 warning` with `.codex_pydeps` on `PYTHONPATH` and workspace-local basetemp.
  - Initial bare `python` invocation was unavailable on this Windows PATH; reran with the bundled Codex Python runtime.
- [x] L-04 current architecture slice focused verification
  - `python -m compileall -q brain_alpha_ops\research\pipeline.py brain_alpha_ops\research\pipeline_state.py tests\test_pipeline_state.py`
  - `python -m pytest tests\test_pipeline_state.py tests\test_strategy_switch.py -q -p no:cacheprovider --basetemp .pytest_tmp_l04`
  - Passed: 7 focused tests, 1 existing pytest config warning.
- [x] Post-refactor quality verification
  - `scripts/check_module_size.py --json`: passed, 151 files checked, no findings.
  - `scripts/quality_gate.py --skip-tests --json`: passed all configured gate steps.
  - Related pytest slice: `42 passed, 1 existing pytest config warning`.
- [x] L-04A extraction smoke verification
  - `python -m compileall -q brain_alpha_ops\research\pipeline.py brain_alpha_ops\research\pipeline_snapshot.py brain_alpha_ops\research\pipeline_state.py tests\test_pipeline_snapshot.py`
  - Focused pytest slice: `27 passed, 1 existing pytest config warning`.
- [x] L-04B snapshot regression and quality verification
  - `scripts/check_module_size.py --json`: passed, 152 files checked, no findings; `pipeline.py` reduced to 3059 lines.
  - `scripts/quality_gate.py --skip-tests --json`: passed all configured gate steps.
  - Related pytest slice: `41 passed, 1 existing pytest config warning`.
- [x] L-04C slot snapshot focused verification
  - `python -m compileall -q brain_alpha_ops\research\pipeline.py brain_alpha_ops\research\pipeline_snapshot.py tests\test_pipeline_snapshot.py`
  - Focused pytest slice: `25 passed, 1 existing pytest config warning`.
- [x] L-04 final verification after all current slices
  - `scripts/check_module_size.py --json`: passed, 152 files checked, no findings; `pipeline.py` at 3073 lines under the 3210 baseline.
  - `scripts/quality_gate.py --skip-tests --json`: passed all configured gate steps.
  - Related pytest slice: `42 passed, 1 existing pytest config warning`.
- [x] L-04D cleanup verification
  - Removed legacy slot snapshot references; `rg "_slot_snapshot_legacy_inline|_slot_message|_slot_progress_percent"` found no active references in `pipeline.py`.
  - `python -m compileall -q brain_alpha_ops\research\pipeline.py brain_alpha_ops\research\pipeline_snapshot.py tests\test_pipeline_snapshot.py`
  - Focused pytest slice: `25 passed, 1 existing pytest config warning`.
- [x] L-04D final gate verification
  - `scripts/check_module_size.py --json`: passed, 152 files checked, no findings; `pipeline.py` at 3017 lines under the 3210 baseline.
  - `scripts/quality_gate.py --skip-tests --json`: passed all configured gate steps.
  - Related pytest slice: `42 passed, 1 existing pytest config warning`.
- [x] Official context lineage validation slice
  - Added focused coverage for complete context, stale metadata as P1, and Dataset field-count mismatch as blocking.
  - `python -m brain_alpha_ops.compliance.redline_verifier --block --json`: passed, `73/73` checks.
  - Related pytest slice: `23 passed, 1 existing pytest config warning`.
- [x] Final diagnosis-upgrade validation
  - Related scoring, guided UX, history, diagnostics, official-context, and quality-gate tests: `33 passed, 1 existing pytest config warning`.
  - Standard `scripts/quality_gate.py --skip-tests --json`: passed; it enforces blocking context lineage while exposing stale official metadata as `P1`.
  - Strict production freshness gate `scripts/quality_gate.py --strict-official-context --skip-tests --json`: correctly failed because all three official context metadata files expired on 2026-05-23 UTC.
  - Full pytest suite: `645 passed, 1 existing pytest config warning`.
- [x] Contract-gate focused verification
  - `fetch_official_context.py --config config/run_config.json --json`: correctly failed with `MISSING_CREDENTIALS` and preserved existing cached official context files.
  - `scripts/check_brain_contract.py --config config/run_config.json --json`: passed in default blocking-only mode; no P0 drift, with P1 stale refresh findings.
  - `scripts/check_brain_contract.py --config config/run_config.json --strict-freshness --json`: correctly failed until a credential-backed official refresh succeeds.
  - Focused pytest slice: `23 passed, 1 existing pytest config warning`.
- [x] Final verification after contract-gate integration
  - `python -m compileall -q brain_alpha_ops scripts tests`: passed.
  - `scripts/quality_gate.py --skip-tests --json`: passed with `brain_contract_validation` included.
  - `scripts/quality_gate.py --strict-official-context --skip-tests --json`: correctly failed on stale official metadata and missing BRAIN credentials.
  - Full pytest suite: `647 passed, 1 existing pytest config warning`.
- [x] Credential-backed official context refresh and strict production closure
  - `fetch_official_context.py --config config/run_config.json --json`: passed; refreshed official fields=7642, operators=66, datasets=16; manifest stale=false; no credentials were written into persisted status.
  - `scripts/check_official_context.py --config config/run_config.json --strict-freshness --json`: passed, blocking_count=0, p1_count=0, dataset_field_count_sum=7642.
  - `scripts/check_brain_contract.py --config config/run_config.json --strict-freshness --json`: passed, redline=73/73, api_output_deviation=0.0, findings=[].
  - Regenerated `docs/ALPHA_PRODUCTION_DIAGNOSIS_20260522.md`; verdict is PASS and unfinished checklist is empty.
  - Focused regression slice: `31 passed, 1 existing pytest config warning`.
  - `scripts/quality_gate.py --strict-official-context --skip-tests --json`: passed all configured gate steps, including strict context validation and diagnostic report sync.
  - `scripts/check_module_size.py --json`: passed, 160 files checked, no oversized-module findings.
  - Full pytest suite: `648 passed, 1 existing pytest config warning`.
  - Sensitive artifact scan: passed, 80 files checked, no findings; a historical local memory note with a password fragment was redacted to `<redacted-password>`.
- [x] L-04E/L-04F/L-04G/L-04H/L-04I architecture-slice verification
  - Pipeline cloud focused slice: `tests/test_pipeline_cloud.py tests/test_pipeline.py tests/test_pipeline_snapshot.py tests/test_backtest_finalization.py` passed, `33 passed, 1 existing pytest config warning`.
  - Web session focused slice: `tests/test_web_session.py tests/test_web_security.py` plus session-related Web/server/dispatch tests passed, `26 passed, 1 existing pytest config warning`.
  - Web HTML/CSP focused slice: `tests/test_web_html.py` plus HTML/CSP/build-inline Web tests passed, `12 passed, 1 existing pytest config warning`.
  - Web error/progress focused slice: `tests/test_web_errors.py tests/test_web_progress.py` plus Web error/get/dispatch tests passed, `17 passed, 1 existing pytest config warning`.
  - Candidate-pool state focused slice: `tests/test_candidate_pool.py` plus backtest-slot pipeline tests passed, `12 passed, 1 existing pytest config warning`.
  - `scripts/check_module_size.py --json`: passed, 165 files checked; `pipeline.py` at 2936 lines and `web.py` at 1448 lines under existing guardrail baselines.
- [x] Final architecture-slice production gate verification
  - `python -m compileall -q brain_alpha_ops scripts tests`: passed.
  - `scripts/check_text_encoding.py --root . --json`: passed, 332 files checked, no findings.
  - `scripts/check_official_context.py --config config/run_config.json --strict-freshness --json`: passed; fields=7642, operators=66, datasets=16, Dataset field-count lineage `7642 == 7642`.
  - `scripts/check_brain_contract.py --config config/run_config.json --strict-freshness --json`: passed; redline `73/73`, API output deviation `0.0`, blocking_count=0.
  - `scripts/quality_gate.py --strict-official-context --skip-tests --json`: passed all configured gate steps, including diagnostic report sync and sensitive artifact scan.
  - Full pytest suite: `664 passed, 1 existing pytest config warning`.
- [x] L-04J/L-04K/L-04L architecture-slice verification
  - New official-context and observability service tests: `9 passed, 1 existing pytest config warning`.
  - Existing pipeline observability regression slice: `3 passed, 1 existing pytest config warning`.
  - Web snapshot runtime regression slice: `15 passed, 1 existing pytest config warning`.
  - `python -m compileall -q brain_alpha_ops scripts tests`: passed.
  - `scripts/check_text_encoding.py --root . --json`: passed, 337 files checked, no findings.
  - `scripts/check_module_size.py --json`: passed, 168 files checked; `pipeline.py` at 2555 lines and `web.py` at 1381 lines under existing guardrail baselines.
  - `scripts/check_official_context.py --config config/run_config.json --strict-freshness --json`: passed; fields=7642, operators=66, datasets=16, Dataset field-count lineage `7642 == 7642`.
  - `scripts/check_brain_contract.py --config config/run_config.json --strict-freshness --json`: passed; redline `73/73`, API output deviation `0.0`, blocking_count=0.
  - `scripts/quality_gate.py --strict-official-context --skip-tests --json`: passed all configured gate steps, including strict official context, BRAIN contract validation, frontend checks, diagnostic report sync, and sensitive artifact scan.
  - Full pytest suite: `673 passed, 1 existing pytest config warning`.
- [x] L-04M Web CSS/source-size final verification
  - `brain_alpha_ops/web/build_inline.py --check --json`: passed; inlined `css/app.css` plus 16 JavaScript modules, no missing sources.
  - `scripts/check_frontend_syntax.py --html brain_alpha_ops/web/index.html --json`: passed, 16 scripts checked, no syntax failures.
  - `scripts/check_frontend_innerhtml.py --json`: passed, 23 approved sinks checked, no findings.
  - `scripts/check_module_size.py --json`: passed, 168 source files checked, no oversized-module findings; `index_template.html` is 439 lines and `app.js` is 995 lines.
  - Frontend/build regression slice: `63 passed, 1 existing pytest config warning`.
  - `scripts/quality_gate.py --strict-official-context --skip-tests --json`: passed all configured gate steps, including strict official context, BRAIN contract validation, CSS+JS inline sync, frontend syntax, module-size audit, diagnostic report sync, and sensitive artifact scan.
- [x] L-04N frontend app-service final verification
  - `brain_alpha_ops/web/build_inline.py --check --json`: passed; inlined `css/app.css` plus 19 JavaScript modules, no missing sources.
  - `scripts/check_frontend_syntax.py --html brain_alpha_ops/web/index.html --json`: passed, 19 scripts checked, no syntax failures.
  - `scripts/check_frontend_innerhtml.py --json`: passed, 23 approved sinks checked, no findings; approved result-table sinks moved out of `app.js`.
  - `scripts/check_module_size.py --json`: passed, 171 source files checked, no oversized-module findings; `app.js` is 781 lines under the default 800-line guard.
  - Frontend/build/Web regression slices: `tests/test_web_frontend_modules.py tests/test_web_build_inline.py` passed `9 passed`; `tests/test_web_frontend_v2.py` passed `50 passed`; `tests/test_web.py` passed `71 passed`, each with the existing pytest config warning.
  - `python -m compileall -q brain_alpha_ops scripts tests`: passed.
  - `scripts/quality_gate.py --skip-tests --json`: passed all blocking gate steps, including frontend inline sync, syntax, innerHTML guard, module-size audit, diagnostic report sync, and sensitive artifact scan.
  - Full pytest suite: `674 passed, 1 existing pytest config warning`.
- [x] Credential-backed strict official-context refresh on 2026-05-25
  - `fetch_official_context.py --config config/run_config.json --json`: passed with session-cookie auth; refreshed fields=7642, operators=66, datasets=16, manifest_complete=true, manifest_stale=false.
  - `scripts/check_official_context.py --config config/run_config.json --strict-freshness --json`: passed with blocking_count=0, p1_count=0, dataset_field_count_sum=7642.
  - `scripts/check_brain_contract.py --config config/run_config.json --strict-freshness --json`: passed with redline `73/73`, API output deviation `0.0`, and no findings.
  - Regenerated `docs/ALPHA_PRODUCTION_DIAGNOSIS_20260522.md`; verdict remains PASS and now records `Official refresh: status=metadata_verified`.
  - `scripts/quality_gate.py --strict-official-context --skip-tests --json`: passed all configured gate steps, including strict official context, strict BRAIN contract validation, diagnostic report sync, and sensitive artifact scan.
