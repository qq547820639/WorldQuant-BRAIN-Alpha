# 无障碍审计：BRAIN Alpha Ops 前端

**标准**：WCAG 2.1 AA | **日期**：2026-05-15  
**页面**：`brain_alpha_ops/web/index.html`（单页应用）

---

## 摘要

| 类别 | 数量 |
|------|------|
| 发现问题总数 | **15** |
| 🔴 Critical | 1 |
| 🟡 Major | 9 |
| 🟢 Minor | 5 |

---

## 1. Perceivable（可感知）

| # | 问题 | WCAG 准则 | 严重度 | 建议 |
|---|------|----------|--------|------|
| 1 | warning Toast 文字 (#a16207) 在黄色底 (#fef3c7) 上对比度 4.42:1，差 0.08 未达 4.5:1 | 1.4.3 | 🟢 Minor | 将 warning 文字色加深至 #8b5000 (+0.3:1) |
| 2 | muted 文本 (#64748b) 在 body 背景 (#eef3f8) 上对比度 4.26:1，未达 4.5:1 | 1.4.3 | 🟢 Minor | 将 muted 加深至 #5a6d80 或背景减淡 |
| 3 | 边框线 (#d7dee8) 在白色面板上对比度 1.35:1，低于 UI 组件最低 3:1 | 1.4.11 | 🟡 Major | 将 line 色值加深至 #bcc3cc (+0.8:1→3.1:1) |
| 4 | 进度条 fill 使用纯色无图案，色觉障碍用户无法区分 warn/bad 进度 | 1.4.1 | 🟢 Minor | 在 warn/bad 进度条上叠加斜线纹理 |
| 5 | 表格无 `<caption>` 或 `aria-label` 描述表格用途 | 1.3.1 | 🟡 Major | 添加 `aria-label="Alpha 候选池"` 或隐藏 `<caption>` |

---

## 2. Operable（可操作）

| # | 问题 | WCAG 准则 | 严重度 | 建议 |
|---|------|----------|--------|------|
| 6 | **表格行仅 `onclick`，键盘用户无法选中/查看详情** | 2.1.1 | 🔴 Critical | 为 `<tr>` 添加 `tabindex="0"` + `role="row"` + Enter/Space 键盘事件 |
| 7 | 弹窗关闭后焦点未回到触发元素 | 2.4.3 | 🟡 Major | `closeDetailModal()` 末尾调用 `previousFocus.focus()` |
| 8 | 弹窗打开时背景内容仍可 Tab 到达（无焦点陷阱） | 2.4.3 | 🟡 Major | 弹窗打开时将焦点锁定在 `.modal-panel` 内 |
| 9 | 按钮 `min-height: 34px`，低于 WCAG 建议的 44px 最小触控目标 | 2.5.5 | 🟡 Major | 将按钮最小高度提升至 44px，或增加内边距扩大点击区域 |
| 10 | 侧栏 sticky 区域无跳过链接，键盘用户需 Tab 穿过全部控件 | 2.4.1 | 🟢 Minor | 在 `<body>` 顶部添加 "跳到内容" 链接 |

---

## 3. Understandable（可理解）

| # | 问题 | WCAG 准则 | 严重度 | 建议 |
|---|------|----------|--------|------|
| 11 | Toast 通知缺少 `role="alert"` 或 `aria-live`，屏幕阅读器不自动播报 | 3.3.1 | 🟡 Major | 给 `.toast-container` 添加 `aria-live="polite"`，error toast 加 `role="alert"` |
| 12 | 进度更新（同步/检查 ETA）无屏幕阅读器通知 | 3.3.1 | 🟡 Major | 为进度文字元素添加 `aria-live="polite"` |
| 13 | 确认弹窗缺少 `role="dialog"` 和 `aria-modal="true"` | 3.2.2 | 🟡 Major | 添加 `role="dialog" aria-modal="true" aria-labelledby="confirmText"` |

---

## 4. Robust（健壮）

| # | 问题 | WCAG 准则 | 严重度 | 建议 |
|---|------|----------|--------|------|
| 14 | 主布局缺少 ARIA 地标：`<header>` / `<main>` / `<aside>` / `<section>` 未标注 role | 4.1.2 | 🟡 Major | 添加 `role="banner"`, `role="main"`, `role="complementary"` |
| 15 | 状态卡（insight-item）使用 `<button>` 但部分用 `<div>` 包覆，角色不清 | 4.1.2 | 🟢 Minor | 统一使用 `<button>`，或显式添加 `role="button" tabindex="0"` |

---

## 5. 色彩对比度精确检查

| 元素 | 前景色 | 背景色 | 对比度 | 要求 | 结果 |
|------|--------|--------|--------|------|------|
| 正文 (#172033 on #ffffff) | #172033 | #ffffff | 16.27:1 | 4.5:1 | ✅ |
| 正文 on body bg | #172033 | #eef3f8 | 14.57:1 | 4.5:1 | ✅ |
| muted 文字 on 白色 | #64748b | #ffffff | 4.76:1 | 4.5:1 | ✅ |
| **muted 文字 on body bg** | #64748b | #eef3f8 | **4.26:1** | 4.5:1 | ❌ |
| teal 主色 on 白 | #0f766e | #ffffff | 5.47:1 | 3.0:1 | ✅ |
| 白字 on teal 按钮 | #ffffff | #0f766e | 5.47:1 | 4.5:1 | ✅ |
| 红色 on 白色 | #b91c1c | #ffffff | 6.47:1 | 4.5:1 | ✅ |
| 绿色 on 白色 | #15803d | #ffffff | 5.02:1 | 4.5:1 | ✅ |
| 棕色 on 白色 | #a16207 | #ffffff | 4.92:1 | 4.5:1 | ✅ |
| **棕色 on 黄色底** | #a16207 | #fef3c7 | **4.42:1** | 4.5:1 | ❌ |
| 蓝色 on 白色 | #2563eb | #ffffff | 5.17:1 | 4.5:1 | ✅ |
| Toast 成功 | #15803d | #dcfce7 | 4.57:1 | 4.5:1 | ✅ |
| Toast 错误 | #b91c1c | #fee2e2 | 5.30:1 | 4.5:1 | ✅ |
| **Toast 警告** | #a16207 | #fef3c7 | **4.42:1** | 4.5:1 | ❌ |
| Toast 信息 | #1d4ed8 | #dbeafe | 5.49:1 | 4.5:1 | ✅ |
| **边框线** | #d7dee8 | #ffffff | **1.35:1** | 3.0:1 | ❌ |

**结论**：核心文本对比度表现优秀（14.57-16.27:1）。4 处未达标均在边缘场景（warning 色调、低对比度 UI 组件、muted 文本在灰色背景上），修复成本低。

---

## 6. 键盘导航审计

| 元素 | Tab 顺序 | Enter/Space | Escape | 方向键 |
|------|---------|-------------|--------|--------|
| 输入框/选择器 | 按 DOM 顺序 ✅ | 聚焦/展开 ✅ | 关闭下拉 ✅ | 选项切换 ✅ |
| 按钮 | 按 DOM 顺序 ✅ | 触发点击 ✅ | — | — |
| 状态卡 | Tab 可达 ✅ | 触发视图切换 ✅ | — | — |
| **表格行** | **不可 Tab** ❌ | **无键盘事件** ❌ | — | — |
| 弹窗关闭 | Tab 可达 ✅ | 触发关闭 ✅ | **无 Esc 监听** ❌ | — |
| 确认弹窗 | Tab 可达 ✅ | 触发按钮 ✅ | **无 Esc 监听** ❌ | — |
| 搜索框 | Tab 可达 ✅ | 聚焦 ✅ | — | — |

**关键缺口**：表格行无键盘可访问性（#6，Critical），弹窗无 Esc 关闭。

---

## 7. 屏幕阅读器审计（模拟）

| 元素 | 预期播报 | 问题 |
|------|---------|------|
| header | "BRAIN Alpha Ops banner" | 无 `role="banner"` → 播报为 generic |
| 侧栏 | "导航 complementary" | 无 `role="complementary"` |
| 表格 | "Alpha 候选池 表格" | 无 `aria-label` 或 `<caption>` |
| 状态卡 | "候选池 按钮 5个" | 部分用 `<div>` 需显式 role |
| Toast 错误 | "提交失败" | 无 `role="alert"` → 不自动播报 |
| 进度更新 | "同步完成 100%" | 无 `aria-live` → 不实时播报 |
| 弹窗 | "详情 dialog" | 无 `role="dialog"` → 播报为 generic |

---

## 8. 优先修复建议

### P0 — 上线前必须修复
1. **表格行键盘访问** (#6, Critical) — 当前键盘用户完全无法操作表格数据

### P1 — QA 期间优先
2. **ARIA 地标** (#14) + **弹窗角色** (#13) — 屏幕阅读器导航基础
3. **Toast 实时播报** (#11) — 错误/成功状态需即时通知
4. **弹窗焦点管理** (#7, #8) — 焦点陷阱 + 关闭后恢复
5. **进度更新播报** (#12) — 长时间操作需告知进度

### P2 — 正式版前
6. 4 处对比度微调 (#1, #2, #3, #4)
7. 触控目标扩大 (#9)
8. 跳过链接 (#10)
9. 角色统一 (#15)
