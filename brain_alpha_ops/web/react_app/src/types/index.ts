/** Core TypeScript type definitions for the BRAIN Alpha Ops web console. */

// All type definitions, type guards, and error-classification helpers live in
// the single merged `allTypes.ts` module.  This barrel re-exports everything so
// that consumers can keep importing from '@/types' unchanged.
export * from './allTypes';
